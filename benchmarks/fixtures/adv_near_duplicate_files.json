{
  "name": "adv_near_duplicate_files",
  "category": "session",
  "items": [
    {
      "id": "user_task",
      "content": "Fix off-by-one in src/util/range.py clamp() — high bound should be exclusive.",
      "content_type": "message",
      "source": "user"
    },
    {
      "id": "file_v1",
      "content": "def clamp(x, low, high):\n    if x < low:\n        return low\n    if x > high:\n        return high\n    return x\n",
      "content_type": "code",
      "source": "src/util/range.py"
    },
    {
      "id": "file_v2",
      "content": "def clamp(x, low, high):\n    if x < low:\n        return low\n    if x >= high:\n        return high\n    return x\n",
      "content_type": "code",
      "source": "src/util/range.py"
    },
    {
      "id": "pytest_output",
      "content": "tests/test_range.py::test_clamp_exclusive FAILED\nE   AssertionError: assert 10 == 9\nE   high bound inclusive bug",
      "content_type": "log",
      "source": "pytest",
      "metadata": {"is_error": true}
    }
  ],
  "must_contain": ["clamp", "AssertionError"],
  "must_preserve": ["exclusive", ">="],
  "must_match": ["range"]
}
