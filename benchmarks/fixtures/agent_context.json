{
  "name": "agent_context_session",
  "items": [
    {
      "id": "system",
      "content": "You are a senior software engineer. Follow project conventions.",
      "content_type": "message",
      "source": "system",
      "metadata": {"content_role": "instruction"}
    },
    {
      "id": "user_task",
      "content": "Fix the authentication bug in src/auth/login.py. The login endpoint returns 500 when password contains special characters.",
      "content_type": "message",
      "source": "user"
    },
    {
      "id": "file_auth",
      "content": "import hashlib\nimport re\nfrom typing import Optional\n\nclass AuthService:\n    def __init__(self, db):\n        self.db = db\n\n    def login(self, email: str, password: str) -> Optional[dict]:\n        user = self.db.find_user(email)\n        if not user:\n            return None\n        hashed = hashlib.sha256(password.encode()).hexdigest()\n        if user.password_hash != hashed:\n            return None\n        return {\"id\": user.id, \"email\": user.email}\n\n    def validate_password(self, password: str) -> bool:\n        return len(password) >= 8 and re.match(r'^[a-zA-Z0-9]+$', password)\n",
      "content_type": "code",
      "source": "src/auth/login.py"
    },
    {
      "id": "pytest_output",
      "content": "tests/test_auth.py::test_special_chars FAILED\nE   AssertionError: Expected 200, got 500\nE   Special chars in password: p@ssw0rd!\n\n1 failed, 42 passed",
      "content_type": "log",
      "source": "pytest",
      "metadata": {"is_error": true}
    },
    {
      "id": "git_status",
      "content": "On branch fix/auth-special-chars\nChanges not staged for commit:\n  modified:   src/auth/login.py\n  modified:   tests/test_auth.py\n\nUntracked files:\n  debug.log\n  tmp_output.txt\n  .env.local\n  node_modules/\n  dist/\n  coverage/\n  .pytest_cache/\n  __pycache__/\n  venv/\n  .idea/\n  .vscode/\n  package-lock.json\n  yarn.lock\n  .DS_Store\n  Thumbs.db\n  *.pyc\n  *.pyo\n  *.swp\n  *.swo\n  *~\n  .coverage\n  htmlcov/\n  .mypy_cache/\n  .ruff_cache/\n  build/\n  *.egg-info/\n  .tox/\n  .nox/\n  pip-log.txt\n  pip-delete-this-directory.txt\n",
      "content_type": "terminal",
      "source": "git status"
    },
    {
      "id": "duplicate_read",
      "content": "import hashlib\nimport re\nfrom typing import Optional\n\nclass AuthService:\n    def __init__(self, db):\n        self.db = db\n\n    def login(self, email: str, password: str) -> Optional[dict]:\n        user = self.db.find_user(email)\n        if not user:\n            return None\n        hashed = hashlib.sha256(password.encode()).hexdigest()\n        if user.password_hash != hashed:\n            return None\n        return {\"id\": user.id, \"email\": user.email}\n",
      "content_type": "code",
      "source": "src/auth/login.py",
      "metadata": {"is_duplicate": false}
    },
    {
      "id": "old_notes",
      "content": "TODO: remove this legacy auth module. The old version is no longer used since v2 migration.",
      "content_type": "text",
      "source": "notes.md",
      "metadata": {}
    }
  ],
  "must_contain": ["AuthService", "AssertionError", "login.py"],
  "must_preserve": ["special characters", "500"]
}
